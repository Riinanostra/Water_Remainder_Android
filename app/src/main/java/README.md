# java

Kotlin/Java source root for the Android app.

## Conventions
- Packages follow the `com.example.waterreminder` namespace.
- UI code lives under `ui/`, data access under `data/`, and business logic under `domain/`.

